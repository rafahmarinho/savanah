import React from 'react';
import styles from '../styles/Footer.module.css'; // Importando os estilos do footer

const Footer: React.FC = () => {
  return (
    <div className={styles.footer}>
      <p>🦁 Desenvolvido por Rafael Marinho 🦁</p>
    </div>
  );
};

export default Footer;
